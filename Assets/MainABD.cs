using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainABD : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Merge Abdullah");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
